# Application
DATABASE_URL="postgresql://USER:PASSWORD@HOST:5432/DATABASE"
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
SITE_URL="http://localhost:3000"

# Auth (server-only unless prefixed NEXT_PUBLIC_)
AUTH_SECRET="replace-with-at-least-32-random-bytes"
AUTH_URL="http://localhost:3000"
AUTH_THROTTLE_SECRET="replace-with-a-separate-random-secret"
AUTH_TRUST_HOST="true"
# Enable only behind a trusted reverse proxy that replaces forwarded IP headers.
AUTH_TRUST_PROXY_HEADERS="false"
# Railway replaces X-Real-IP. Change this only for a reviewed proxy/CDN topology.
AUTH_CLIENT_IP_HEADER="x-real-ip"
AUTH_ALLOWED_ORIGINS=""
AUTH_GOOGLE_ENABLED="false"
AUTH_GOOGLE_ID=""
AUTH_GOOGLE_SECRET=""
AUTH_EMAIL_FROM="Haus of Estate <noreply@hausofestate.com>"

# Product features. Keep disabled until their respective launch gates are met.
# Sign-in and account access are deferred until their backend is ready.
AUTH_ENABLED="false"
SAVED_CONTENT_ENABLED="false"
PROPERTY_ASSISTANT_ENABLED="false"
AI_GATEWAY_API_KEY=""
PROPERTY_ASSISTANT_MODEL="openai/gpt-5.6-luna"
PROPERTY_ASSISTANT_USAGE_SECRET="replace-with-a-separate-random-secret"
PROPERTY_ASSISTANT_DAILY_TOKEN_BUDGET="500000"
# Exact HTTPS citation hosts used by assistant-approved Sanity guidance.
PROPERTY_ASSISTANT_SOURCE_HOSTS="hausofestate.com,www.hausofestate.com"

# Exact pending-migration set. Leave empty until backup/schema checks and approval.
HAUS_DATABASE_MIGRATIONS_APPROVED=""

# Content
NEXT_PUBLIC_SANITY_PROJECT_ID="jdxbkry4"
NEXT_PUBLIC_SANITY_DATASET="production"
SANITY_STUDIO_DATASET="staging"
# Server read token enables authenticated, signed Sanity preview.
SANITY_API_READ_TOKEN=""
# Optional Viewer-only token for standalone live draft updates; never an editor token.
SANITY_API_BROWSER_TOKEN=""
SANITY_REVALIDATE_SECRET=""
NEXT_PUBLIC_SANITY_SOCIAL_CAMPAIGNS_ENABLED="false"
SANITY_WRITE_TOKEN=""

# Optional local-only property review bundle (absolute path outside the repository).
# Used only by /dev/property-previews while NODE_ENV=development.
HAUS_PROPERTY_PREVIEW_DIR=""

# Bot protection (disabled until Cloudflare/privacy approval)
BOT_PROTECTION_ENABLED="false"
TURNSTILE_SITE_KEY=""
TURNSTILE_SECRET_KEY=""
# Comma-separated exact hostnames only; no schemes, ports, paths, or wildcards.
TURNSTILE_ALLOWED_HOSTNAMES=""

# Email
RESEND_API_KEY=""
# Verified company sender shared by careers and optional lead notifications.
RESEND_FROM_EMAIL="noreply@hausofestate.com"
ADMIN_EMAIL="info@hausofestate.com"
# Website applications remain disabled until hosted HR delivery is verified.
# Careers reuses AUTH_TRUST_PROXY_HEADERS, AUTH_CLIENT_IP_HEADER and configured
# site/auth origins. Its IP/email throttle is bounded per-process best effort;
# provider abuse controls and hosted inbox receipt must be checked before enabling.
# No database or LEAD_RATE_LIMIT_SECRET is required for careers email submission.
CAREERS_INTAKE_ENABLED="false"
CAREERS_EMAIL="careers@hausofestate.com"

# Analytics
NEXT_PUBLIC_GTM_ID=""
# Optional exact hostnames for a controlled production-build demo; no schemes,
# ports or wildcards. Empty defaults to hausofestate.com,www.hausofestate.com.
NEXT_PUBLIC_ANALYTICS_ALLOWED_HOSTS=""

# Phase-one lead intake. Both flags are disabled by default.
LEAD_INTAKE_ENABLED="false"
LEAD_DELIVERY_ENABLED="false"
LEAD_DELIVERY_PROVIDER="powerautomate"
# Providers: powerautomate (legacy default), zeptomail, or resend.
# Resend reuses RESEND_API_KEY / RESEND_FROM_EMAIL and requires LEAD_NOTIFICATION_TO.
LEAD_ALLOWED_ORIGINS="http://localhost:3000"
LEAD_RATE_LIMIT_SECRET="replace-with-a-long-random-value"

# Educational purchase-readiness guide. This never enables payment collection.
PURCHASE_READINESS_ENABLED="false"

# Power Automate delivery. Keep every value server-only.
POWER_AUTOMATE_FLOW_URL=""
POWER_AUTOMATE_TENANT_ID=""
POWER_AUTOMATE_CLIENT_ID=""
POWER_AUTOMATE_CLIENT_SECRET=""
POWER_AUTOMATE_SCOPE="https://service.flow.microsoft.com//.default"

# Optional ZeptoMail operational notifications; does not replace auth/careers Resend.
# Set all values explicitly and confirm the receiving inbox before enabling delivery.
# Copy the full regional /v1.1/email URL from the account's SMTP/API tab.
ZEPTOMAIL_API_URL=""
ZEPTOMAIL_SEND_MAIL_TOKEN=""
ZEPTOMAIL_FROM_EMAIL=""
LEAD_NOTIFICATION_TO="info@hausofestate.com"

# Additive Google Sheets destination. Both delivery flags must be explicitly true.
# Keep disabled until the company-approved spreadsheet/tab and service account are verified.
# New enquiries always retain a pending Sheets row; disabled delivery does not discard it.
LEAD_SHEETS_ENABLED="false"
LEAD_SHEETS_SPREADSHEET_ID=""
LEAD_SHEETS_TAB_NAME=""
# Server-only service-account JSON (type, client_email, private_key); never NEXT_PUBLIC_.
LEAD_GOOGLE_SERVICE_ACCOUNT_JSON=""

# Confirmed bookings: public service-page URL and official iframe src only.
# No API, OAuth, or admin URLs. Keep disabled until the actual team calendar is configured and tested.
SNAGGING_BOOKING_ENABLED="false"
SNAGGING_BOOKING_URL=""
SNAGGING_BOOKING_EMBED_URL=""

# Optional worker tuning. Railway runs the one-shot worker every five minutes.
LEAD_DELIVERY_BATCH_SIZE="20"
LEAD_DELIVERY_MAX_ATTEMPTS="8"
LEAD_DELIVERY_LEASE_SECONDS="600"
LEAD_DELIVERY_BASE_DELAY_SECONDS="300"
LEAD_DELIVERY_MAX_DELAY_SECONDS="21600"
LEAD_DELIVERY_TIMEOUT_MS="8000"
